{
  "name": "collevento-monorepo",
  "private": true,
  "scripts": {
    "copy-resources": "node scripts/copy-resources.js",
    "generate-manifest": "node scripts/generate-manifest.js",
    "build": "npm run copy-resources && npm run generate-manifest && npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "@vercel/kv": "^1.0.0",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.8.17"
  }
}
