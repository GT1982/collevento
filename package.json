{
  "name": "collevento-monorepo",
  "private": true,
  "scripts": {
    "generate-manifest": "node scripts/generate-manifest.js",
    "build": "npm run generate-manifest && npm install --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0",
    "redis": "^4.7.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.8.17"
  }
}
